<%= form_for(@repository, url: {action: :new},) do |f| %>
  <% if @repository.errors.any? %>
    <div class="alert">
      <ul>
      <% @repository.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title, class: 'right' %>
    <%= f.text_field :title, size: 40, :autocomplete => :off%>
  </div>
  <div class="field">
    <%= f.label :pass, class: 'right' %>
    <%= f.password_field :password, size: 40, :autocomplete => :off%>
  </div>
  <div class="field">
    <%= f.label :password_confirmation, class: 'right' %>
    <%= f.password_field :password_confirmation, size: 40, :autocomplete => :off%>
  </div>
  <div class="field">
    <%= f.label :description, class: 'right' %>
    <%= f.text_area :description, size: '52x6', :autocomplete => :off%>
  </div>
  <div class="actions">
    <%= f.submit translate(:repo_btn_create)  %>
  </div>
<% end %>

<% content_for :info do %>
  <div id="info">
    <p><%=translate :repo_form_info%></p>
    <div id="clear"></div>
  </div>
<% end %>